# 호스트 경로. docker compose 실행 전에 수정하거나 export 한다.
DATA_DIR=/hdd/watermark/dataset/images
WEIGHT_DIR=/hdd/watermark/model
HF_CACHE=${HOME}/.cache/huggingface

# FLUX.1-dev / FLUX.1-schnell VAE 다운로드용 (gated면 필수)
HF_TOKEN=

# Docker / 학습용 의존성
# torch, torchvision 은 Dockerfile에서 CUDA wheel로 설치한다.
# numpy 는 PyTorch 이미지(conda) 것을 그대로 쓴다.

pytorch-lightning==2.4.0
omegaconf==2.3.1
einops==0.8.2
pandas
six
Pillow
kornia==0.8.2
scipy
scikit-image
opencv-python==4.8.1.78
Wand
lpips==0.1.4
diffusers==0.40.0
transformers==5.13.0
accelerate==1.14.0
huggingface-hub==1.28.0
safetensors
taming-transformers-rom1504
tqdm
regex
filelock
requests
PyYAML
